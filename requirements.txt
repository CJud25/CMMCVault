# Pinned to the exact validated set so the tested environment is reproducible from
# this file (Python 3.14; app boot + all 63 tests verified green 2026-07-05).
# Bump deliberately: re-run `python -m pytest` and smoke-test app.py after any change.
streamlit==1.58.0
pandas==3.0.3
python-docx==1.2.0
